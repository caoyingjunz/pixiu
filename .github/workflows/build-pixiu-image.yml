name: Publish pixiu-server image

on:
  push:
    branches:
      - "master"
    paths-ignore:
      - 'docs/**'
      - 'cmd/cluster-agent/**'
      - 'docker/Dockerfile-cluster-agent'
      - 'docker/Dockerfile-toolbox'
      - 'deploy/agent/**'
      - '.github/workflows/build-cluster-agent-image.yml'
      - '.github/workflows/build-toolbox-image.yml'

jobs:
  build-amd64:
    runs-on: ubuntu-latest # 原生 amd64
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Checkout pixiu-ui
        uses: actions/checkout@v3
        with:
          repository: pixiu-io/pixiu-ui
          ref: master
          # GITHUB_TOKEN 无法访问其他私有仓库，需配置有 pixiu-ui 读权限的 PAT
          token: ${{ secrets.PIXIU_UI_TOKEN }}
          path: pixiu-ui

      - name: Get short commit hash
        run: |
          echo "COMMIT_HASH=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          echo "TIMESTAMP=$(date +%Y%m%d%H%M%S)" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login harbor
        uses: docker/login-action@v3
        with:
          registry: crpi-0ecikjs9ylb2hqyo.cn-hangzhou.personal.cr.aliyuncs.com
          username: ${{ secrets.DOCKER_NAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push the amd64 pixiu image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile
          platforms: linux/amd64
         # platforms: linux/amd64,linux/arm64
          build-args: |
            VERSION=${{ env.COMMIT_HASH }}-${{ env.TIMESTAMP }}
          push: true
          provenance: false
          sbom: false
          tags: |
            crpi-0ecikjs9ylb2hqyo.cn-hangzhou.personal.cr.aliyuncs.com/pixiu-public/pixiu:latest
<<<<<<< HEAD
            crpi-0ecikjs9ylb2hqyo.cn-hangzhou.personal.cr.aliyuncs.com/pixiu-public/pixiu:v2.0.1
=======
            crpi-0ecikjs9ylb2hqyo.cn-hangzhou.personal.cr.aliyuncs.com/pixiu-public/pixiu:v2.0.2-beta.1
>>>>>>> 6fc7e03813457c6b86f45d318578c5a87f68d131
        # crpi-0ecikjs9ylb2hqyo.cn-hangzhou.personal.cr.aliyuncs.com/pixiu-public/pixiu:${{ env.COMMIT_HASH }}

  build-arm64:
    runs-on: ubuntu-24.04-arm # GitHub 原生 arm64 hosted runner（public preview），无需 QEMU
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Checkout pixiu-ui
        uses: actions/checkout@v3
        with:
          repository: pixiu-io/pixiu-ui
          ref: master
          # GITHUB_TOKEN 无法访问其他私有仓库，需配置有 pixiu-ui 读权限的 PAT
          token: ${{ secrets.PIXIU_UI_TOKEN }}
          path: pixiu-ui

      - name: Get short commit hash
        run: |
          echo "COMMIT_HASH=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          echo "TIMESTAMP=$(date +%Y%m%d%H%M%S)" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login harbor
        uses: docker/login-action@v3
        with:
          registry: crpi-0ecikjs9ylb2hqyo.cn-hangzhou.personal.cr.aliyuncs.com
          username: ${{ secrets.DOCKER_NAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push the arm64 pixiu image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile
          platforms: linux/arm64
          build-args: |
            VERSION=${{ env.COMMIT_HASH }}-${{ env.TIMESTAMP }}
          push: true
          provenance: false
          sbom: false
          tags: |
<<<<<<< HEAD
            crpi-0ecikjs9ylb2hqyo.cn-hangzhou.personal.cr.aliyuncs.com/pixiu-public/pixiu:v2.0.1-arm64
=======
            crpi-0ecikjs9ylb2hqyo.cn-hangzhou.personal.cr.aliyuncs.com/pixiu-public/pixiu:v2.0.2-beta.1-arm64
>>>>>>> 6fc7e03813457c6b86f45d318578c5a87f68d131
