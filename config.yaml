default:
  # 服务监听端口
  listen: 8090
  # jwt 签名的 key
  jwt_key: pixiu

# 数据库地址信息
mysql:
  # 打印 sql 执行语句
  enable_log: true
  host: peng
  user: root
  password: password123456
  port: 3306
  name: pixiu
